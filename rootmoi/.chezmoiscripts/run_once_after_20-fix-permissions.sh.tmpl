#!/bin/bash
# {{ template "scripts-library" }}
log_task "Rootmoi: 20. Fixing permissions"

# After chezmoi finished installing it, we can add execution permission to it.
chmod +x /usr/local/share/gcm/git-credential-manager.exe
