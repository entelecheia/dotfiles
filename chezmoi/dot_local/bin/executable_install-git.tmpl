#!/bin/bash
# {{ template "scripts-library" }}
log_task "Installing git"

# TODO: install curl and expat
# TODO: get the latest version tags from curl and expat
# wget https://github.com/curl/curl/releases/download/curl-8_1_1/curl-8.1.1.tar.gz
# tar -xf curl-8.1.1.tar.gz
# cd curl-8.1.1
# ./configure --prefix=$HOME/.local
# make
# make install

# wget https://github.com/libexpat/libexpat/releases/download/R_2_5_0/expat-2.5.0.tar.gz
# tar -xf expat-2.5.0.tar.gz
# cd expat-2.5.0
# ./configure --prefix=$HOME/.local
# make
# make install

# TODO: install git
# GIT_SHARE_DIR="${HOME}/.local/share/git"
# GIT_SHARE_REPO="https://github.com/git/git.git"
# if [ ! -d "${GIT_SHARE_DIR}" ]; then
#     log_task "Cloning ${GIT_SHARE_REPO}"
#     c git clone "${GIT_SHARE_REPO}" "${GIT_SHARE_DIR}"
# else
#     log_task "Updating ${GIT_SHARE_DIR}"
#     c cd "${GIT_SHARE_DIR}"
#     c git pull
# fi
# c cd "${GIT_SHARE_DIR}"
# c make configure
# c ./configure --prefix="${HOME}/.local" --with-curl=$HOME/.local/curl --with-expat=$HOME/.local/expat
# c make
# c make install
