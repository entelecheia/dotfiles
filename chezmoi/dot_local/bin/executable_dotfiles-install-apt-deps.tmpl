#!/bin/bash
# {{ if and (eq .chezmoi.os "linux") (.system.is_sudoer) }}
# {{ template "scripts-library" }}
# {{ template "install-apt-dependencies" }}

# {{ else }}

log_info "Skipping install-apt-dependencies because not running as root or not on Linux."

# {{ end }}
