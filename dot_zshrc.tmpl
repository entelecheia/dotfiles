#!/bin/sh
# adding chezmoi to PATH
export PATH="$HOME/bin:$PATH"

# export HOST=$(uname -n)
export LANG='en_US.UTF-8'
export LANGUAGE='en_US:en'
export LC_ALL='en_US.UTF-8'
export TERM=xterm-256color

# export PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin

# [ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# export PYENV_ROOT="$HOME/.pyenv"
# export PATH="$PYENV_ROOT/bin:$PATH"
# eval "$(pyenv init --path)"
# eval "$(pyenv init -)"

# if which pyenv-virtualenv-init > /dev/null; then
#   eval "$(pyenv virtualenv-init -)"
# fi

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
# [[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

# export PATH="$HOME/.poetry/bin:$PATH"

#THIS MUST BE AT THE END OF THE FILE FOR SDKMAN TO WORK!!!
# export SDKMAN_DIR="/Users/yj.lee/.sdkman"
# [[ -s "/Users/yj.lee/.sdkman/bin/sdkman-init.sh" ]] && source "/Users/yj.lee/.sdkman/bin/sdkman-init.sh"

# GitHub hub
# eval "$(hub alias -s)"

# NVM
# export NVM_DIR="$HOME/.nvm"
# [ -s "/opt/homebrew/opt/nvm/nvm.sh" ] && . "/opt/homebrew/opt/nvm/nvm.sh"  # This loads nvm

# lfs-folderstore
# export PATH="$HOME/.bin:$PATH"

# shell integration for iterm2
# source ~/.iterm2_shell_integration.zsh

# adding subl to PATH, so we can use it from Terminal
# PATH="${PATH}:/Applications/Sublime Text.app/Contents/SharedSupport/bin"

# export PYENV_VIRTUALENV_DISABLE_PROMPT=1

##### Zsh/Oh-my-Zsh Configuration
# export ZSH="/Users/yj.lee/.oh-my-zsh"

# ZSH_THEME="spaceship-prompt/spaceship"
# source "/usr/local/opt/spaceship/spaceship.zsh"
# plugins=(git git-prompt zsh-autosuggestions zsh-completions )

# source $ZSH/oh-my-zsh.sh

# add MacTex Path
# PATH="${PATH}:/Library/TeX/texbin"
